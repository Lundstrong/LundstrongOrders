{
  "extends": [
    "config:base"
  ],
  "regexManagers": [
    {
      "fileMatch": ["^foreman.toml$"],
      "matchStrings": [".+ = { source = \"(?<depName>.+)\", version = \"(?<currentValue>.+)\" }"],
      "extractVersion": "^v?(?<version>.*)$",
      "datasourceTemplate": "github-releases"
    }
  ]
}
